name: Deploy Women Safety Route

on:
  workflow_dispatch:  # manual trigger
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Hosting
        run: |
          firebase deploy \
            --project women-safest-route-finder \
            --token 1//0gtm_Kuo2w6SuCgYIARAAGBASNwF-L9Ir82ay1Hqb7XOJW7xYUhtEvA9yD7CatKw8KV5ahGKu_NHb_JKBNtvp9YXgrOaZwPJxKhk
            --non-interactive
